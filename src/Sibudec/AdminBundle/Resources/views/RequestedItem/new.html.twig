{% extends '::base.html.twig' %}

{% block body -%}
    <h1>RequestedItem creation</h1>

    <form action="{{ path('admin_requesteditem_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_requesteditem') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
