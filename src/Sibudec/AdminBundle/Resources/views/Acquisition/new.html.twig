{% extends '::base.html.twig' %}

{% block body -%}
    <div class="content pure-g">
        <div class="content-header pure-g">
            <div class="pure-u-1-2">
                <h2 class="content-title">Adquisiciones</h2>
                <p class="content-subtitle">
                    Complete el formulario para agregar nueva adquisición.
                </p>
            </div>
            <div class="pure-u-1-2 content-controls">
                <a class="pure-button secondary-button" href="{{ path('admin_acquisition') }}">Volver</a>
            </div>
        </div>
        <div class="content-body pure-u-1">
            <form action="{{ path('admin_acquisition_create') }}" method="post" {{ form_enctype(form) }} class="pure-form pure-form-stacked">

                {{ form_errors(form) }}

                <div>
                    {{ form_label(form.title) }}
                    {{ form_errors(form.title) }}
                    {{ form_widget(form.title) }}
                </div>
                <div>
                    {{ form_label(form.author) }}
                    {{ form_errors(form.author) }}
                    {{ form_widget(form.author) }}
                </div>
                <div>
                    {{ form_label(form.year) }}
                    {{ form_errors(form.year) }}
                    {{ form_widget(form.year) }}
                </div>
                <div>
                    {{ form_label(form.bibliography) }}
                    {{ form_errors(form.bibliography) }}
                    {{ form_widget(form.bibliography) }}
                </div>
                <div>
                    {{ form_label(form.school) }}
                    {{ form_errors(form.school) }}
                    {{ form_widget(form.school) }}
                </div>

                {{ form_rest(form) }}

                <p>
                    <button type="submit" class="pure-button success">Guardar</button>
                </p>
            </form>
        </div>
    </div>
{% endblock %}
